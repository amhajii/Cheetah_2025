// #include <iostream>
#include<bits/stdc++.h>
using namespace std;

#define L(i,j,k) for (int i = j; i <= k; i++)
#define R(i,j,k) for (int i = j; i >= k; i--)
#define vecInt vector<int>

#define push push_back
#define pop pop_back

#define all(x) (x).begin(), (x).end()


int main(){
    
    system("clear");

    char ch = '5';
    int num = ch - '0';
    
    char ss[] = {ch};
    int mm = stoi(ss);
    
    
    
    int num = stoi(string(1, ch));
    
    
    // int num = atoi(string(1, ch).c_str());
    


    string str;
    getline(cin, str);

    return 0;
}